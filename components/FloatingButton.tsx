"use client";

export default function FloatingButton() {
  return (
    <a
      href="#"
      className="fixed bottom-20 right-6 z-50 hidden lg:block bg-white text-primary border-2 border-primary rounded-full px-5 py-2.5 text-sm font-semibold shadow-lg hover:bg-primary hover:text-white transition-all duration-300"
    >
      고객문의
    </a>
  );
}
